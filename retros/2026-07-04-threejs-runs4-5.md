<!-- review-status: pending -->
# Retro — three.js · voxel→realistic HVAC pipeline (RUNS 4-5, §8 third+fourth reopen) · 2026-07-04 · Research-SDD self-retrospective

> Run reviewed: RUNS 4-5 — reopen commit `aabc777` through STOP commit `bdc2735` (B22-B32, 11
> iterations, +§14 correction `c4f290b`). Trigger: corpus-level STOP, read-only-investigable = 0
> (genuine exhaustion — "32/32 gaps across 5 runs, no further iterations pending"). Prior retros
> `2026-07-04-threejs.md` (RUN 1), `-run2.md` (RUN 2), `-run3.md` (RUN 3) already reviewed and NOT
> re-litigated here except where runs 4-5 produced new, directly relevant evidence.
> Method: a FRESH-CONTEXT agent read the current kit (`PROMPT-LOOP.md` + `METHODOLOGY.md`) FIRST —
> confirming which of RUN 1-3's proposed deltas are now baked into the kit — then all three prior
> retros, then runs 4-5's commits/blocks/RESEARCH-STATE.md/INDEX.md/TARGETS.md/SOURCES.md/`tools/probe.mjs`,
> and proposes kit deltas. READ-ONLY on the kit — this report only PROPOSES; kit changes are
> human-reviewed and human-committed (METHODOLOGY §18).

## Proposed kit deltas

| # | Proposed change | Target (file · §/section) | Evidence (block / commit / § / transcript ref) | Type | Priority |
|---|---|---|---|---|---|
| 1 | Add a **browser/WebGL dynamic-phase probe recipe** for WEB targets, as a sibling to `toolbelt/DYNAMIC-SETUP.md` + `toolbelt/probe.sh` (both are device/protocol-oriented — TCP reachability, mirrored networking, byte-level protocol probes — with nothing for a rendering target). Codify three concrete gotchas hit on this run's first-ever dynamic phase: (a) **WSL Chrome needs explicit software-GL flags** — default flags fail WebGL context creation (`BindToCurrentSequence failed`); `--use-angle=swiftshader --enable-unsafe-swiftshader` is required. (b) **`file://` cannot run the ES-module prototypes** (CORS blocks module imports) — a local HTTP server is mandatory, not optional. (c) **Software-GPU FPS is not representative and must be excluded**, while **draw-call/triangle API counts remain exact** regardless of GPU backend — state this distinction explicitly so a future dynamic phase doesn't discard good data alongside the bad. | `toolbelt/DYNAMIC-SETUP.md` (new §4 "Browser/WebGL probes") or a new `toolbelt/BROWSER-PROBE.md` + a `tool-registry.md` row | `threejs-block26.md` §26.2 ("WebGL context creation FAILED... required `--use-angle=swiftshader --enable-unsafe-swiftshader`"; "`file://` cannot run the prototypes at all (ES-module CORS)"; "FPS is NOT representative... Draw-call and triangle counts ARE exact"); `tools/probe.mjs` lines 20-21 (the launch flags) and line 27 (`http://localhost:8123/` — the working server pattern), preserved as this run's reusable artifact. | new | HIGH |
| 2 | The **MCP-doc snapshot rule** (METHODOLOGY §5, "Docs retrieved via an MCP server... A LOAD-BEARING `[CERT-web]`-via-MCP claim MUST be snapshotted to `sources/web-snapshots/`") is now adopted kit law (accepted from RUN 1's retro), yet has **zero real-world compliance across every run since adoption**: `sources/web-snapshots/` holds 71 files and NOT ONE is a context7 fragment; runs 4-5 alone cite `context7` 17 times across 6 blocks (B23, B24×5, B25×2, B28×2, B30×5, B31×2) with no snapshot for any of them. The rule exists on paper but has no gate forcing it, unlike the token-check/marker-tally items in §11's self-verify contract. Add an explicit self-verify checklist line: "load-bearing MCP-doc citations snapshotted to `sources/web-snapshots/`? (Y/N, count)" — the same treatment `[CERT]` token-checking already gets. | `METHODOLOGY.md §11` (self-verify contract, new checklist bullet) | `sources/SOURCES.md` lines 20-24 (the context7 policy note, never executed); `ls sources/web-snapshots/ \| grep -ci context7` = 0; per-block context7 counts (`grep -c context7 threejs-block2{3,4,5,8}.md threejs-block3{0,1}.md` = 1,5,2,2,5,2). This is the SAME gap RUN 1's retro first flagged (delta #2) and RUN 2's retro confirmed still-open for context7 specifically — now confirmed a THIRD time, across TWO more runs, after the rule was formally adopted. The novelty this retro adds: it is no longer a proposal-not-yet-reviewed problem, it is an **adopted-but-unenforced rule** problem, which points at a different fix (a checklist gate) than simply re-stating the policy a fourth time. | refinement | HIGH |
| 3 | Name **"next-iteration archive audit"** as a standing expected step of the ORCHESTRATED-AUTO archive phase (PROMPT-LOOP step 6 / METHODOLOGY §6): because each iteration in this mode is a FRESH sub-agent that reads `INDEX.md`/`RESEARCH-STATE.md` from scratch (no inherited memory), it is naturally positioned to notice the PRECEDING iteration's bookkeeping gaps or factual slips while adding its own entry — this happened twice, unprompted, in this run. Formalize it as a one-line expected check ("before appending your own entry, does the preceding iteration's INDEX/RESEARCH-STATE entry look complete and consistent? If not, repair it as part of this iteration's archive step") rather than leaving it as an accident of the mode. This is DISTINCT from §14 (which audits KNOWLEDGE CLAIMS) and §17 (which resumes after a kill/crash) — neither covers ARCHIVE-BOOKKEEPING consistency (INDEX layer tables, file-count totals) between one iteration's close and the next's open. | `PROMPT-LOOP.md` step 6 (UPDATE STATE) or `METHODOLOGY.md §6`/§11 (new sub-bullet) | Commit `5f7b51c` (B26, driver-supervised inline per §12) updated the G26 checkbox in INDEX.md but did NOT add a "Layer 6" block-table row for itself; commit `e104421` (B27, delegated sonnet sub-agent) added BOTH the missing Layer-6 section AND its own B27 row in the same diff (`git show e104421 -- INDEX.md`). Separately: `threejs-block24.md`'s own self-verify sweep ("grep sweep across all 23 files") surfaced the corpus's true file count is 25, not 23 — driver-verified and corrected in `c4f290b` ("flagged by B24 audit, driver-verified"), a §14 correction whose SOURCE was an ordinary iteration's routine grep, not a dedicated audit step. | new | MEDIUM-HIGH |
| 4 | `TARGETS.md` master-table registration (RUN 1's delta #1, since applied) is a **one-time write, not a living mirror** — it goes stale as the target's own corpus grows or self-corrects. Three.js's row 55 still reads `**mature** (21 md / git yes / hook yes)` and "23 voxel/realistic HVAC files" after runs 4-5 added 11 more blocks (32 total) AND after `c4f290b` corrected the file count to 25 — the kit's own registry of this target is now stale on BOTH the block count and the exact fact `c4f290b` fixed inside the target. Add a rule: a §14 correction or a run-STOP that changes a fact already mirrored in `TARGETS.md` (file/block counts, run counts, retro counts) MUST refresh that row as part of closing the correction/run, not leave it for a future hand-edit. | `TARGETS.md` (row 55 + the "### 13 — three.js" narrative, lines 109-114) + `METHODOLOGY.md §15`/§18 (a clause near the STOP declaration or §14 correction step) | `TARGETS.md:55` ("21 md... 23 voxel/realistic HVAC files") vs `RESEARCH-STATE.md`/`INDEX.md` (32 blocks, 25 files, confirmed current); `TARGETS.md:109-114` ("3 runs: 21 blocks... 3 retros in retros/") — all three numbers (runs, blocks, retros) are now wrong (should be 5, 32, 4-with-this-one). | new | MEDIUM |
| 5 | METHODOLOGY §16 (multi-focus corpus) is written for genuinely PARALLEL subjects on one target (niagara's Spyder / OptimizerSupervisor / platform-native) and does not fit this target — three.js has stayed ONE continuous axis (the same angle declared at bootstrap) across 5 reopens, so §16's per-focus `RESEARCH-STATE-<focus>.md` split does not apply and should NOT be forced onto it. But the underlying SCALING problem §16 solves for parallel focuses (one file growing unboundedly) is now ALSO happening within this single focus: one `RESEARCH-STATE.md` carries a 32-row gap-backlog table and a 32-row iteration-history table, accreted across 5 separate reopen events in one file, with no compaction. Add a distinct rule (not §16, which is about parallel AXES) for a single, repeatedly-reopened focus: once a reopen crosses a size/count threshold (e.g., a 3rd+ reopen, or >25 iteration-history rows), collapse PRIOR runs' rows into a one-line-per-run summary (blocks, gaps closed, coverage ratio, retro link) and keep only the CURRENT run's rows verbose. | `METHODOLOGY.md §8` (reopen paragraph, new sub-clause distinguishing "parallel-focus scaling" (§16) from "single-focus repeated-reopen scaling") | `RESEARCH-STATE.md`: gap-backlog table has 32 rows (G1-G32) spanning runs 1-5 in reverse-then-forward order (G11/G13/G15 interleaved among G20-G32 non-sequentially — already hard to scan); Iteration-history table has 32 rows in one un-collapsed sequence; git log shows 4 distinct reopen commits (`eec7f27`, `98c9f2e`, `aabc777` [covering both runs 4 AND 5]) all mutating the SAME `RESEARCH-STATE.md`. | refinement | MEDIUM |

For each delta above, one line of rationale (WHY it matters, what it costs, expected impact):

- **#1** — This is the kit's FIRST dynamic phase on a purely web/browser target (all prior dynamic-phase experience is device/protocol, per `DYNAMIC-SETUP.md`'s network/probe framing) — without a recorded recipe, the next web-target dynamic phase re-discovers the same three environment failures (WebGL context creation, CORS, GPU representativeness) from scratch. Cost: one short section + a preserved reference script (`tools/probe.mjs` already exists as the concrete artifact). Impact: turns three hard-won environment fixes into a reusable playbook, the same service #1 in RUN 3's retro provided for web-fetch fallbacks.
- **#2** — This is the highest-value delta in this retro precisely because it is NOT a rediscovery: it is the same gap RUN 1 and RUN 2 already named, now shown to persist through TWO more runs and 17 more citations after the kit adopted the rule — meaning restating the rule a fourth time would not fix it. The fix that would (a checklist line inside the SAME self-verify gate that already reliably catches token/marker issues) is cheap and directly closes a 5-run-old open loop.
- **#3** — Zero cost beyond a sentence in the archive step, but it converts an accident (two iterations happened to catch problems left by a prior one) into a designed property of ORCHESTRATED-AUTO — valuable specifically BECAUSE §11 tells the driver to TRUST self-reports and never re-verify with Bash: without SOME organic backstop, an INDEX/RESEARCH-STATE drift (like the missing Layer-6 row) could persist indefinitely once trust replaces re-verification.
- **#4** — Cheap (a row + three numbers) but the miss is structurally interesting: the exact same session that CORRECTED the file count inside the target (`c4f290b`) left the kit's own registry of that same fact uncorrected — a correction that doesn't propagate to its own upstream registration is a narrower, more concrete case of "state can silently diverge" than RUN 1's original registration gap.
- **#5** — Distinguishing this from §16 matters because forcing a genuinely single-axis target into a multi-focus split would be the WRONG fix (it isn't parallel subjects) — but the file is still going to keep growing at the same rate every future reopen, so a dedicated single-focus scaling rule is needed rather than silently relying on §16 to eventually apply.

## Already covered (dedupe — proof the retro read the kit first and against all three prior retros)

- **Bundle-evidence-quality rule (RUN 2 delta #1), MCP-doc citation rule (RUN 1 delta #2, before this run's compliance finding), web-fetch fallback ladder (RUN 3 delta #1), identity-negatives taxonomy (RUN 3 delta #2), MCP-capability tool-registry logging (RUN 3 delta #3)** — all five are now present verbatim in `METHODOLOGY.md` (§5 "Bundle-evidence quality", §5 "Docs retrieved via an MCP server", §5 "Web-fetch fallback ladder", §8 "Identity negatives ≠ feature absence", §10 "MCP-server capabilities count as tools too") and in `toolbelt/tool-registry.md` (chrome-devtools MCP row) — confirmed by reading the current kit files. Runs 4-5 did not need to rediscover any of these; not re-proposed. (Delta #2 above is a NARROWER, NEW finding about one of these five — compliance, not the rule's existence — not a duplicate.)
- **`TARGETS.md` registration (RUN 1 delta #1)** — confirmed APPLIED: row 55 exists (unlike runs 1-3, where this was still `grep`-empty). This is the first retro that can mark it applied rather than pending. (Delta #4 above is about the row going STALE post-application, a distinct, later-stage problem from the original absence.)
- **Reopening a STOPPED loop for a bounded multi-gap expansion (RUN 2 deltas #2/#3, now folded into METHODOLOGY §8's "Reopening a STOPPED loop" paragraph, which explicitly cites "three.js run 2: four new gaps G16-G19... OR a bounded MULTI-GAP expansion")** — runs 4-5's reopen (`aabc777`: G22-G28 active + G29-G32 pre-queued for a later run, in ONE commit) is a further, LARGER instance of the same already-accepted pattern; the kit's own wording already anticipates it. Not re-proposed.
- **PAUSED-vs-STOPPED vocabulary (RUN 1 delta #5, RUN 2 deltas #2/#3, now codified in METHODOLOGY §8 "PAUSED (budget-cap) ≠ STOPPED")** — RUN 4's transition into RUN 5 was correctly worded as "RUN 4 gaps complete" (not "STOPPED", since G29-G32 were already pre-authorized/queued in the SAME `aabc777` reopen, not a fresh authorization) and RUN 5's genuine end was correctly worded "RUN 5 STOP: investigable=0". No ambiguity recurred; the codified rule is being followed correctly.
- **§12 Dynamic phase — supervised, not loop-blind, invasiveness ladder, read-first** — B26 correctly ran as a rung-1 read-only probe, driver-supervised (not delegated: `no · driver-supervised (§12) + Puppeteer probes` recorded in RESEARCH-STATE's iteration table, matching §12's explicit instruction that `no·inline` is the COMPLIANT tier record for this phase, not a skipped delegation). Correctly applied, not re-proposed.
- **§14 cross-block correction, applied correctly** — the file-count fix (`c4f290b`) followed the full contract: kept the original text, added "corrected in B24/§14", cited the new evidence, updated RESEARCH-STATE/INDEX. Textbook application; not re-proposed (only its SOURCE — an ordinary iteration surfacing it — is new, per delta #3).
- **Coverage metric as a ratio (32/32), not a free-floating %** → METHODOLOGY §8/§9.6, consistently followed across all 5 runs.
- **Corpus language English, no override** → METHODOLOGY §9, consistent.
- **Delegated sweeps with declared model tier** — all 9 delegated iterations in runs 4-5 recorded `yes · sonnet (full iteration)`; the one inline exception (B26, §12) correctly recorded `no · driver-supervised` per the rule above, not a silently-skipped tier declaration.

## Anti-patterns observed

- Zero context7/MCP snapshots landed across 32 blocks and 5 runs despite the rule being adopted kit law since RUN 1 → delta #2 proposes the concrete gate that would actually enforce it, since three runs of re-stating the policy in prose has not.
- `TARGETS.md`'s per-target description is treated as a bootstrap-time artifact and never revisited, even by the same session that corrected one of the exact facts it states → delta #4.
- One operational detail named in this retro's brief — a `pkill` command matching its own invoking shell during the B26 dynamic-phase session, recovered via a `[b]racket` grep pattern — could NOT be independently corroborated from the committed corpus (git log, blocks, RESEARCH-STATE, SOURCES.md all searched, no trace). This class of ephemeral shell/tooling hiccup during a driver-supervised dynamic phase leaves no citable evidence by design (§12 preserves PROBE OUTPUT as `[CERT-hw]`, not the driver's own shell-command mishaps) — noted honestly rather than built into a delta on unverifiable grounds. If this pattern recurs and the maintainer independently confirms it happened, it would be a small, cheap addition to `toolbelt/DYNAMIC-SETUP.md`'s "Common gotchas" list (delta #1's target file) — but is not asserted here as a corpus-evidenced finding.
- `threejs-block32.md` §32.2 ("Floor-plan-to-3D: the technique the corpus already uses") shows an EXTERNAL-domain research gap (BIM/IFC, run 5) organically surfacing and back-citing an existing-but-previously-uncited local technique (`cuarto-frio-plano-realistic (6).html`'s hardcoded plan-polygon wall extrusion) — a valid secondary path to the same backfill outcome METHODOLOGY §13's coverage audit describes, just arriving via a design-comparison gap instead of a dedicated audit sweep. Not proposed as a separate delta (the outcome — citing local prototype code accurately — is already required by golden rule 3; only the DISCOVERY PATH is worth a one-line mention if the maintainer wants §13 to name it), flagged here for completeness rather than duplicated as delta #6.

## Metrics

- **Blocks reviewed**: 11 (B22-B32) + reopen commit `aabc777` + correction commit `c4f290b` + `RESEARCH-STATE.md` + `INDEX.md` + `TARGETS.md` + `SOURCES.md` + `tools/probe.mjs` + all three prior retros (for dedupe)
- **§14 cross-block corrections in this run**: 1 (`c4f290b`, file-count 23→25, correctly executed per the existing contract)
- **Rules skipped in practice**: 0 hard READ-ONLY/marker/citation/§12-supervision violations found; 1 confirmed multi-run compliance gap (MCP-doc snapshot rule, adopted but never executed — delta #2); 1 registry-staleness gap (delta #4)
- **Deltas proposed (new)**: 3 (#1, #3, #4)  ·  **Deltas proposed (refinement)**: 2 (#2, #5)
- **Already-covered lessons**: 9 (including one — TARGETS.md registration — newly confirmed APPLIED rather than still-pending, a first for this target's retro series)

## Honest verdict

This run did NOT break any hard rule: READ-ONLY discipline, citation/marker discipline, the §12
supervised/no-loop-blind dynamic-phase contract, §14's correction protocol, and the ORCHESTRATED-AUTO
mode's own description in PROMPT-LOOP were all followed correctly across 11 iterations with zero gate
failures. Five of RUN 1-3's proposed deltas are now confirmed baked into the kit and were correctly
NOT needed again this run — including three.js's own original registration gap (RUN 1 delta #1),
now the first of this target's four deltas to flip from "pending" to "applied and confirmed."
What IS genuinely new: this run's first-ever dynamic phase on a purely web/browser target exposed
three environment gotchas the kit's device/protocol-oriented dynamic-phase tooling has no place for
(#1); the MCP-doc snapshot rule — accepted kit law since RUN 1 — is now shown to have a 100%
non-compliance rate across 5 runs and 32 blocks, which reframes it from "still needs review" to
"needs an enforcement gate, not another restatement" (#2); ORCHESTRATED-AUTO's fresh-per-iteration
design produced an emergent, twice-observed self-auditing behavior (a later iteration repairing an
earlier one's archive bookkeeping) that is worth naming as a designed property rather than a lucky
accident (#3); the kit's own cross-repo registry of this target went stale on exactly the fact a
same-session correction fixed inside the target, a narrower case than the original registration gap
(#4); and the target's single continuous focus, reopened 4 times into one un-collapsing
`RESEARCH-STATE.md`, is hitting a scaling problem that resembles but is NOT §16's multi-focus case
and needs its own rule (#5). One item from the retro's own brief — the `pkill` self-match incident —
could not be corroborated from any committed artifact and is reported as unverifiable rather than
built into a delta, per the honesty clause.
