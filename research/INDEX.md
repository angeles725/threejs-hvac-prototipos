# Three.js library (HVAC voxel→realistic pipeline) — Mental Model · Master Index

**Updated**: 2026-07-04
**Analyzed system**: Three.js JavaScript 3D library (r160 primary, r128 legacy) as used by the
HVAC equipment prototype corpus in this directory (25 standalone HTML files: 7 realistic + 18 voxel — corrected per B24/§14; earlier blocks citing 23 inherit the correction).
**Method**: Empirical READ-ONLY research (Research-SDD). Tools: direct reading of prototypes,
context7 MCP (`/mrdoob/three.js`), WebSearch/WebFetch over threejs.org + official wiki,
fetch-doc.sh for preservation. Block style and markers: see `research-sdd/METHODOLOGY.md`.

This index guides through the **40 blocks** of this research. Each block is an independent
`.md`, linked to the rest with `[Block K]`. The auto-generated flat catalog lives in
[CATALOG.md](CATALOG.md) (regenerate with `python3 tools/gen-catalog.py`).

## Marker legend

`[CERT]` local primary · `[CERT-doc]` official document (sources/) · `[CERT-web]` official web ·
`[CERT-a]` forum/secondary · `[INFER]` deduction. (Detail in `research-sdd/METHODOLOGY.md §3`.)

---

## Layer summary

| Layer | Topic area | Blocks | Status | One-line summary |
|---|---|---|---|---|
| 1 | Corpus baseline & library fundamentals | 1 | active | How the prototypes use three.js today |
| 2 | Voxel stage (instancing, isometric look) | 2, 7 | active | The voxel-art first-pass techniques |
| 3 | Realistic stage (PBR, IBL, shadows, color) | 3-6, 8-9 | active | The realistic second-pass techniques |
| 4 | Cross-cutting (perf, migration, upgrade paths) | 10-11, 13, 22-25, 28 | active | Versioning, performance, postprocessing, PBR value references, product lighting, cheap visual wins, asset optimization pipeline, Blender round-trip |
| 5 | Synthesis: team workflow | 12 | done | Voxel-first → realistic-second pipeline doc (B12 + WORKFLOW.md) |
| 6 | Dynamic phase & performance budgets (run 4) | 26-27 | active | Live-measured baseline (draws/triangles) + device-class budget tables derived from it |
| 7 | HVAC domain (run 5) | 29-32 | done | Digital-twin/equipment-viewer conventions applied to this corpus; dashboard/telemetry data-binding layer; terrain/relief and buildings/BIM for site context |
| 8 | Design system (run 6) | 33 | active | Template-as-a-system architecture proposal: shared `lib/` module + parametric equipment components + export contract to client integration projects |

---

## Full map

### Layer 1 — Corpus baseline & library fundamentals

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 1 | Corpus baseline: how the HVAC prototypes use Three.js | [block1](threejs-block1.md) | versions (r160/r128), importmap/CDN, shared scaffolding, voxel-vs-realistic split, outliers |

### Layer 2 — Voxel stage

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 2 | InstancedMesh and voxel-scale rendering | [block2](threejs-block2.md) | InstancedMesh API contract, needsUpdate/bounding caveats, setColorAt, vs mergeGeometries, legacy anti-pattern, BatchedMesh (new gap) |
| 7 | Cameras and controls | [block7](threejs-block7.md) | fake-isometric low-FOV histogram, OrthographicCamera alternative, OrbitControls update() contract, MapControls option |

### Layer 3 — Realistic stage

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 3 | The PBR material system | [block3](threejs-block3.md) | metallic-roughness workflow, MeshPhysicalMaterial extensions, house material palette, glass transmission-vs-opacity divergence, r163 envMapIntensity change |
| 4 | Lighting and environment | [block4](threejs-block4.md) | RoomEnvironment+PMREM IBL pipeline, house 3-light rig, physically-correct lighting era (r147/r151/r155), candela/decay semantics |
| 5 | Shadows | [block5](threejs-block5.md) | shadow map types, ortho frustum fitting, bias/normalBias, mapSize ladder, castShadow opt-outs, cuarto-3d baked-shadows evolution |
| 6 | Color management and tone mapping | [block6](threejs-block6.md) | r152 color management, working space, rename map, tone-mapping menu (AgX in r160), ACESFilmic exposure calibration, legacy color hole |
| 8 | The geometry toolkit of the realistic stage | [block8](threejs-block8.md) | geometry census (Cylinder 54), constructor contracts, bevelEnabled footgun, curved Shape extrude, procedural Lathe, direct vertex editing |
| 9 | Procedural texturing with CanvasTexture | [block9](threejs-block9.md) | draw-don't-download strategy, fin/nameplate implementations, texture colorSpace contract, missing-SRGBColorSpace divergence, NearestFilter voxel option |

### Layer 4 — Cross-cutting

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 10 | Migration and versioning | [block10](threejs-block10.md) | migration ledger r147-r163, r148 examples/js removal, r161 UMD removal (r160 = last UMD release), legacy upgrade recipe, CDN strategies |
| 11 | Performance: budgets, levers, and BatchedMesh | [block11](threejs-block11.md) | perf inventory, draw-call levers, shadow baking backport, BatchedMesh r160-proven + API + per-palette batching (closes G15) |
| 13 | Rendering methods | [block13](threejs-block13.md) | rAF vs setAnimationLoop vs on-demand rendering, render targets, WebGPURenderer + TSL track, setNodesHandler migration bridge |
| 14 | Case studies I: eight standalone demos | [block14](threejs-block14.md) | silhouette-POM/TSL, WebGPU path tracer, landscape generator, parametric creature configurator, GRAVEBOUND, WebGPU wave, borrow-list for HVAC |
| 15 | Case studies II: dasprinzip tinker series | [block15](threejs-block15.md) | WebGL r169 vs TSL/WebGPU r171-185 split, data-driven instanced grids, GPU particle flow, texture-swap UI, Tweakpane, fullscreen-quad TSL shading |
| 16 | MapLibre GL JS interop | [block16](threejs-block16.md) | BSD-3 map renderer, CustomLayerInterface onAdd/render, shared GL context + MercatorCoordinate, georeferenced equipment use cases |
| 17 | Optimization compendium II | [block17](threejs-block17.md) | LOD + hysteresis (voxel-as-far-LOD idea), frustumCulled semantics, KTX2/Basis compressed textures, disposal contract + ResourceTracker |
| 18 | Post-processing | [block18](threejs-block18.md) | EffectComposer chain + OutputPass, selective bloom (real glow for HMI/LEDs), OutlinePass component highlighting, SSAO/GTAO toggle, AA caveats, on-demand compatible |
| 19 | The asset pipeline: glTF in, glTF out | [block19](threejs-block19.md) | GLTFLoader + Draco/KTX2 legs, auto-framing viewer recipe, GLTFExporter (.glb) as the bridge to map/viewer/DCC, onlyVisible LOD gotcha |
| 20 | Forum intelligence | [block20](threejs-block20.md) | iOS FloatType+LinearFilter gotcha (HalfFloatType fix), 120Hz ProMotion budgets, VAT+instancing, SlugText GPU labels, volumetric 3D-LUT lesson, discourse as ongoing source |
| 21 | Case studies III | [block21](threejs-block21.md) | 7 three.js sites (r128-r185), MeshSurfaceSampler scattering + fat lines (full-source evidence), atlas3d AI-labeled exploded views, 3 honest negatives, cross-batch corroboration |
| 22 | Physically plausible PBR value references | [block22](threejs-block22.md) | Filament metal F0 table + dielectric reflectance table, glTF metallic-binary rule corroboration, corpus palette audit (13/24 entries mid-metalness anti-pattern), corrected palette proposal |
| 23 | Product-lighting design + RectAreaLight | [block23](threejs-block23.md) | key/fill/rim theory + fill ratios, why reflective metal wants large soft sources, RectAreaLight constructor/init/limitations (no shadows, PBR-only), RectAreaLightHelper, house-rig-to-theory mapping, HVAC studio recipe table |
| 24 | Cheap visual wins catalog | [block24](threejs-block24.md) | MeshMatcapMaterial (no-lights fake studio finish), baked aoMap/lightMap (uv2 contract), ShadowMaterial + official blurred-depth contact-shadow technique, vertex colors via setColorAt, gradient/env backgrounds + backgroundBlurriness, FXAA/SMAA/SSAA cost ladder, ranked cost→beauty tables (voxel + realistic) |
| 25 | Asset optimization pipeline: glTF-Transform + gltfpack | [block25](threejs-block25.md) | glTF-Transform CLI `optimize` + à-la-carte commands (draco/meshopt/etc1s/uastc/resize/dedup/prune/weld/simplify), JS API, MIT license, gltfpack meshopt-first single binary, runtime pairing to [Block 19] loader legs (r111+/r122+ floors), recipes for web-viewer/marketing/MapLibre .glb profiles, local capability check (both CLIs confirmed runnable) |
| 28 | Blender ↔ three.js round-trip for organic/baked HVAC parts | [block28](threejs-block28.md) | official glTF 2.0 add-on export contract (+Y up, Apply Modifiers, Draco, GPU-instancing), Principled BSDF → glTF PBR channel mapping (base color/metallic-roughness packing/baked-AO node group/normal/emissive), Cycles bake workflow (AO/Normal bake types, Selected-to-Active, Margin) as the authoring answer to [Block 24]'s uv2 requirement, round-trip recipe (procedural → export → Blender touch-ups/bake → optimize → load), when to author from Blender vs procedural code |

### Layer 5 — Synthesis

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 12 | SYNTHESIS: voxel-first → realistic-second workflow | [block12](threejs-block12.md) | two-pass pipeline evidence, stage roles, shared template economics, punch list; companion WORKFLOW.md |

### Layer 6 — Dynamic phase & performance budgets (run 4)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 26 | DYNAMIC PHASE: measured baseline of the prototypes | [block26](threejs-block26.md) | live Puppeteer/SwiftShader probes, draw-call/triangle counts per prototype, shadow-pass double-draw mechanism, environment caveats (software GPU → FPS excluded) |
| 27 | Performance budgets per device class | [block27](threejs-block27.md) | frame-time math (16.6/8.3 ms), MDN/Unity/community draw-call guidance, adaptive-quality ladder (DPR/quality-toggle/shadow-size), device-class budget tables gap-checked against B26's measured numbers, real-hardware measurement protocol |
| 40 | LOD applied & measured: hotel building far-shell (run 7) | [block40](threejs-block40.md) | `THREE.LOD` auto-update contract (r106+, no manual loop call), singleton-vs-dispersed-InstancedMesh applicability rule, far-shell technique (omit `roomsGroup` interior + swap `transmission` glass for opaque), `[CERT-hw]` before/after (HI 692/921,744 → LO 656/854,424 = −5.2%/−7.3%), honest finding: equipment (chillers+pumps = 65% of equipment tris) dominates the budget, not the building; transmission fillrate cost unmeasured under SwiftShader → G41 equipment-LOD (requires-execution) |

### Layer 7 — HVAC domain (run 5)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 29 | HVAC/industrial equipment visualization domain | [block29](threejs-block29.md) | exploded-view technique (forum consensus + CAD-viewer explosion-value convention), X-ray/ghost mode (depthWrite/renderOrder/fresnel), ISA-101 status-color convention (neutral-default, color-on-abnormal), Blender-empty hotspot pattern (CSS2DObject), Viewer3D vanilla-three.js warehouse-SCADA precedent (InstancedMesh pools + baked vertex colors + render-on-demand), ranked HVAC-viewer feature checklist |
| 30 | Dashboards: 3D scene + telemetry/data binding + charts | [block30](threejs-block30.md) | CSS2DRenderer/CSS2DObject as a second renderer (contract, pointerEvents, cost model) vs DOM overlays vs SlugText, data-binding recipe (emissiveIntensity/setColorAt mutation + on-demand render, 1Hz throttle, WebSocket transport), DOM chart libraries (uPlot/Chart.js/ECharts) around the canvas vs rare in-scene CanvasTexture sparklines, Monash BAS Graphics Standard layout (header/nav/main panes, alarm-console/priority convention, floor-selector ordering), camera-preset deep-links, standalone-HTML-vs-build-step verdict |
| 31 | Terrain and relief for site context | [block31](threejs-block31.md) | CPU heightmap displacement (position attribute + computeVertexNormals) vs material displacementMap (with its "pair with a matching normal map" caveat), Mapbox Terrain-RGB decode formula, AWS Terrain Tiles/Terrarium free public-S3 decode formula, MapLibre native `terrain`+`raster-dem`+`TerrainControl` as an alternative to hand-built relief, cheap terrain texturing (vertex color by altitude/slope, CanvasTexture gradients, satellite draping), three ranked site-context recipes (stylized pad / real-relief patch / full MapLibre terrain) |
| 32 | Buildings/BIM: building shells and floor plans for HVAC context | [block32](threejs-block32.md) | ThatOpen `web-ifc` (WASM IFC parser, MPL-2.0) + `@thatopen/components` (MIT, current `OBC` API, IFC→Fragments convert-once workflow) superseding deprecated `web-ifc-three`, corpus's own floor-plan-to-3D technique in `cuarto-frio-plano-realistic (6).html` (`Shape`/`ShapeGeometry` per-room polygons + axis-aligned box-stacked walls with door cuts), general `ExtrudeGeometry`-along-path form for non-rectilinear plans, image-underlay tracing tools (blueprint3d/blueprint3d-modern/OpenPlan3D), OSM `building`/`height` tags + MapLibre `fill-extrusion` for footprint massing, decision table (IFC vs plan-extrusion vs OSM massing vs MapLibre) tied to the two-pass workflow's context-vs-hero framing — closes G32, the final gap (32/32) |

### Layer 8 — Design system (run 6)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 33 | The template as a system: a design-library architecture proposal | [block33](threejs-block33.md) | copy-paste quantification across 27 prototypes + 4 independent client-integration reinventions (Tridium, Honeywell UpDetail/CarcamoDetail, bms-casino React), `lib/` proposal (scene-kit/palette/rig/equipment/viewer), composite-scene shell kits (room/datacenter/plant), export contract (.glb vs versioned module) with the React-hosted contrast case, migration path ranked by leverage |
| 34 | Motion design with intent | [block34](threejs-block34.md) | easing/duration bands, tween-vs-mixer (corpus uses zero mixers), camera transition patterns, shadow re-bake gotcha, motion vocabulary |
| 35 | 3D interaction UX | [block35](threejs-block35.md) | picking pipeline + drag threshold, hover/focus states, touch mapping, zero-loading-screens finding, camera UX recipes |
| 36 | 2D design tokens + accessibility | [block36](threejs-block36.md) | overlay census (26/27 hardcoded), lib/theme.css proposal, WCAG contrast violation found, ISA-101 status tokens, reduced-motion |
| 37 | Composition & art direction | [block37](threejs-block37.md) | FOV↔focal mapping, diagonal grammar measured (19/21 at 38-48° azimuth), plan-scale FOV gap, preset vocabulary, catalog thumbnail spec |
| 38 | Deliverables + visual QA | [block38](threejs-block38.md) | 4K capture contract (tested live), transparent captures, tools/capture.mjs, pixelmatch golden regression, delivery kit table |
| 39 | Dataviz craft | [block39](threejs-block39.md) | Cleveland-McGill ranking, chart discipline (no gauges), Monash red/green ramp flagged colorblind-risky, per-widget style contract |

### Layer 9 — Procedural generation & numerical methods for design tools (run 9)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 45 | Robust polygon offsetting + straight skeleton | [block45](threejs-block45.md) | the nave build tool's `collapseWalls` union-find collapses only collinear runs, leaving corner/T/X junctions interpenetrating (`[CERT]` file:line); polygon offset = Minkowski-with-disk + join types (Round/Miter/Square/Bevel) + miter_limit acute-angle fallback; boolean union via Vatti + the integer-coordinate robustness trick (Clipper does the maths on scaled ints; pure-JS Martinez uses floats + loop-caps); straight skeleton wavefront + edge/split events + the roof lift (z = slope·time_reached); mature JS libs table (clipper2-wasm Boost/maintained, js-angusj-clipper Clipper1/int-only, StrandedKitty CGAL-WASM skeleton MIT, polygon-clipping Martinez FP); actionable build-tool recommendation (offset→union→straight-skeleton roof, snap to int grid first) |
| 46 | Robust boolean CSG on 3D meshes: three-bvh-csg | [block46](threejs-block46.md) | three-bvh-csg (gkjohnson) v0.0.18 MIT — the de-facto Three.js mesh-CSG addon (core ships none); API `Brush`/`Evaluator.evaluate(a,b,op)` + ops ADDITION/SUBTRACTION/REVERSE_SUBTRACTION/DIFFERENCE/INTERSECTION + HOLLOW_* non-solid; BVH via three-mesh-bvh v0.9.14 → ">100× faster than BSP-based CSG" (vs legacy CSG.js/ThreeCSG BSP tree); HONEST robustness ceiling: INPUT must be two-manifold/watertight, OUTPUT "may not be correctly completely two-manifold" (float precision; coplanar/missing-triangle issues #68/#164; useCDTClipping experimental; drawRange export gotcha); robustness ladder BSP-float → BVH-float → CGAL exact-predicates+constructions; decision rule for the nave: prismatic walls/openings → 2D Clipper union + extrude / Shape.holes (nave v18 already uses ExtrudeGeometry, 0 CSG `[CERT]`); reserve 3D CSG for oblique roof cuts / angled duct penetrations / curved solids |
| 47 | Triangulation: earcut (three.js default) vs Constrained Delaunay | [block47](threejs-block47.md) | verified from source that `ShapeGeometry`/`ExtrudeGeometry` caps triangulate via `ShapeUtils.triangulateShape` → vendored Mapbox **earcut** (`Earcut.triangulate`, ISC), an ear-slicing algo (NOT Delaunay) — nave v18's 7 ExtrudeGeometry sites inherit it, 0 explicit CDT `[CERT]`; MATH: ear-clipping (two-ears theorem, n−2 tris, z-order/spatial-hash + hole-bridging) trades quality for speed → slivers + T-junctions ("harmless for rendering, breaks FEM/navmesh"); vs CDT empty-circumcircle property + `orient2d`/`incircle` predicates + edge-flip legalization → maximizes min angle (no slivers); JS libs compared HONESTLY: **poly2tri** (sweep-line Domiter-Žalik, BSD-3, FLOAT + no input validation → brittle on dup/collinear/touching holes) vs **cdt2d** (MIT, EXACT predicates via robust-in-sphere/robust-orientation, but WIP + needs clean-pslg PSLG); earcut ships own Delaunay `refine()` (flips, but NOT conforming); 3-tier decision rule: flat prismatic cap → earcut as-is (nave = this); visible slivers → earcut+refine(); deformable/simulated/UV/hole-riddled slab → cdt2d over clean-pslg; ties useCDTClipping (B46) = cdt2d; G65 = shared predicate root cause |
| 48 | Robust geometric predicates: float sign-flip near degeneracy + the three fixes (cross-cutting synthesis of B45/B46/B47) | [block48](threejs-block48.md) | THE root cause B45/B46/B47 all pointed at. A predicate is the SIGN of a determinant (`orient2d` 2×2, `orient3d` 3×3, `incircle` 4×4, `insphere` 5×5) and float loses that sign FIRST — Shewchuk: "roundoff error may lead to an incorrect result when the true determinant is near zero… can lead an application to fail" `[CERT-web]`; a sign flip is a LOGICAL contradiction (left-of AND inside), not a magnitude error → sweep-lines loop/crash. Three honest fixes compared: (a) **exact adaptive predicates** (Shewchuk, "industry standard since 1996", adaptive = only as much precision as needed → fast common case) → JS `robust-predicates` (Unlicense), `robust-orientation`+`robust-in-sphere` (MIT, cdt2d's exact deps); (b) **integer-grid snapping** (Clipper2 "using integer coordinates internally… to ensure numerical robustness") → exact integer arithmetic, tradeoffs = scale-factor/overflow/quantisation, Hobby snap-rounding; (c) **epsilon** = fragile patch (absolute ε is scale/unit-dependent, relative ε narrows but doesn't close the degeneracy window). Three.js epsilon VERIFIED in source: `BufferGeometryUtils.mergeVertices(geo, tolerance=1e-4)` is an ABSOLUTE quantised grid (`~~(x·hashMultiplier+hashAdditive)`, magnitude-agnostic, 32-bit `~~` wraps on large coords) `[CERT-web]`; `Vector3.equals` exact, no ε. Build-tool rule: recenter CAD world offset first (nave `origin_shift:[-245.51,-208.1]` `[CERT]`) → snap to integer µm grid BEFORE any boolean → exact predicates for hand-rolled orient/incircle → NEVER absolute ε on large world coords. Honesty: the mythical six-figure nave insert coord was NOT found in corpus (real offset = hundreds of m); principle (georeferenced CAD sits far from origin) stands. G65 CLOSED |
| 49 | Mesh simplification by Quadric Error Metrics (Garland-Heckbert 1997) — the maths, what three.js SimplifyModifier ACTUALLY runs, and the equipment-LOD rule | [block49](threejs-block49.md) | **QEM maths verified in the primary paper**: per-vertex symmetric 4×4 error quadric `Q`, error = quadratic form `vᵀQv`; `Q = Σ Kp` with `Kp = ppᵀ` for each incident plane `p=[a b c d]`, `a²+b²+c²=1` (Eq. 2, §5) `[CERT-doc]`; collapse rule `Q̄=Q1+Q2` (§4); optimal position = solve the 4×4 linear system (quadric with bottom row `(0,0,0,1)`, Eq. 1), else best of segment/endpoints if singular; cost = `v̄ᵀQ̄v̄`; 10 floats/vertex. **PREMISE CORRECTED** (this gap + the task assumed SimplifyModifier "is QEM" — FALSE both eras): classic `SimplifyModifier` @ r160 = **Stan Melax 1998** curvature heuristic `edgelength·curvature+borders` (`r160:10-16,290`) `[CERT]` — borders "not totally correct" & zeroed (`:278-288`), UVs merely copied not in cost (`:403-405`) → smears texture seams; current `dev` `SimplifyModifier` = a **thin wrapper over meshoptimizer** (`:12-13`, `simplifyWithAttributes` w/ normal-weight 0.25, uv-weight 0.5, `:73-74,95`). **meshoptimizer IS the real QEM** — `simplifier.cpp` cites Garland-Heckbert 1997 + Garland 1999 + Hoppe appearance-attribute quadrics 1999/2000 (`:24-30`), `struct Quadric`/`quadricFromPlane`/`quadricFromAttributes` `[CERT-web]`; MIT; attribute-aware (UV weight 10-100), topology/seam-preserving, normalized target_error (`1e-2`=1% extent) + returned deviation for LOD, `simplifySloppy`/`SimplifyPermissive`+vertex-lock variants. **Rule** (feeds G41 equipment LOD): decimate HVAC/furniture at **build time with meshoptimizer** (gltf-transform/gltfpack, [Block 25]) → target-error LODs w/ preserved UV/normal seams, serve via `THREE.LOD` ([Block 40]); runtime `SimplifyModifier` only for simple non-critical-UV geometry (and Melax-damaging on r160 builds). G62 CLOSED |
| 50 | Isosurface extraction: Marching Cubes (1987) vs Dual Contouring (2002) — maths, what three.js MarchingCubes.js runs, JS-ecosystem maturity, HVAC fit | [block50](threejs-block50.md) | **MC**: scalar field on uniform grid → 8-bit `cubeindex` → 256 configs → **15 base cases** (symmetry) → edge/tri tables → **linear-interp crossing** `t=(iso−v0)/(v1−v0)` (verified `mu=(isol−valp1)/(valp2−valp1)` in three.js source); face/interior **ambiguity** → Nielson-Hamann **asymptotic decider** / Chernyaev 33 cases / marching-tetrahedra; **DC** (Ju et al. 2002): one vertex **per cube** placed by minimising a **QEF** `E(x)=Σ(nᵢ·(x−pᵢ))²` over edge-crossing points + **Hermite normals** → **preserves sharp features** MC rounds, needs gradient data, colinear-normal instability (SVD-clamp + centroid bias); three.js `MarchingCubes.js` = fixed-grid CPU metaball surfacer (`isolation=80`, "32 is pushing it", `1/r²` `addBall`, no sharp features); **HONEST maturity**: three.js ships MC only, `isosurface` npm = MC/marching-tetra/**surfaceNets** (dual but NOT feature-preserving), **no maintained JS dual-contouring lib**; HVAC rule: metaballs→MarchingCubes, **temperature/CFD isosurface = real unbuilt datacenter-hotspot/gobernador-aire app**, sharp voxel edges→port DC yourself |
| 51 | Curves & surfaces: Bézier (Bernstein/De Casteljau) · Catmull-Rom (why centripetal is default) · B-spline/NURBS (Cox-de Boor + rational weight) — three.js Curve API, arc-length reparam, Frenet-frame flip, TubeGeometry | [block51](threejs-block51.md) |
| 53 | Objective material-read gate: MEASURED reviewer-variance in design3d + CIEDE2000 ΔE00 anchor (from img2threejs upstream) | [block53](threejs-block53.md) | **DOCUMENT-mode capture** (§20), "design tools" axis. design3d `materials` gate is **reviewer-variance-dominated**: two blind reviewers scored the SAME stainless render (mean-gray **91.4 vs 91.6**, 0.3% apart) **0.80 PASS vs 0.57 FAIL** — measured on `equipos-v2/…/materials-attempt*.review.json` + ImageMagick crop means `[CERT]`. ⇒ "attempts per asset" is NOT a valid kit-version metric. **Fix = CIEDE2000 ΔE00** (img2threejs v1.3 feature, `forge/_shared/color_metrics.py:24-107`, pure `math`, Sharma-verified) as an OBJECTIVE colour anchor: script measures a render-crop mean sRGB vs a **spec-declared `colorTarget`** (their target comes from a reference PHOTO we don't have → move it to the spec), model confirms identity, SCRIPT judges value; gate on `ΔE00 ≤ deltaE00Max` in `gate-state.mjs`. **Validated** (ported to JS): ΔE00(v1.9-recipe-render vs v1.8-PASS)=**0.13** (imperceptible → the FAIL was reviewer noise) while ΔE00(v1.9-spec-render vs same)=**11.7** (catches a REAL spec-vs-recipe material divergence). Traps: sRGB-encoding is load-bearing (our `capture.mjs` PNG is sRGB, valid); explicit crop rect not their auto foreground mask; adopt only the `deltaE00` axis; Apache-2.0 ©2026 hoainho (attribution required). STAGED kit proposal (user promotes). Extends [Block 34]/preserved older Object-Sculptor snapshot which predates CIEDE2000. |
| 52 | Procedural placement: Poisson-disk/blue-noise (Bridson 2007) · WFC (constraint solving, not quantum) · L-systems — feeding InstancedMesh | [block52](threejs-block52.md) | **Bridson O(N)** blue-noise VERIFIED in the SIGGRAPH07 sketch PDF: min-distance `r`, background grid cell `r/√n` (≤1 sample/cell), active list, `k=30` candidates in the `r..2r` annulus, `2N−1` iters → linear; blue noise beats uniform-random (clusters) and grid (anisotropy) — Bridson names all three; **WFC** (Gumin) HONESTLY = constraint propagation NOT quantum ("doesn't do the actual quantum mechanics"), observe lowest-Shannon-entropy cell → collapse → **AC-4** propagation (NB: AC-4 not AC-3; Merrell's ancestor used AC-3), overlapping vs simple-tiled (adjacency), contradiction=all-coeffs-zero→NP-hard→restart (rare); **L-system** = parallel rewriting grammar `G=(V,ω,P)` axiom+productions, turtle interp `F +- [ ]` branching, deterministic/stochastic/parametric/context-sensitive; corpus GROUNDING [CERT]: living-hotel palms hand-placed `PALM_POS` array, bushes/grass UNIFORM-RANDOM-in-rectangle via LCG (= the clustering Bridson warns of, no min-dist); three.js ships ONLY `MeshSurfaceSampler` (area-uniform surface scatter, O(n) build/O(log n) sample, NOT blue-noise) — Poisson/WFC/L-sys are external MIT libs (kchapelier poisson-disk-sampling/fast-2d, wavefunctioncollapse, nylki lindenmayer) whose output fills `InstancedMesh.setMatrixAt`; per-job decision rule (scatter→Poisson, on-surface→MeshSurfaceSampler, tile/building layouts→WFC, plants/pipes→L-system, ≤15 hero→hand-place). Closes G66 = LAST read-only gap of RUN 9 → STOP | **Bézier** = Bernstein blend `B(t)=Σ C(n,i)(1−t)^{n−i}t^i P_i` — VERIFIED literal in `Interpolations.js` (cubic `k³/3k²t/3kt²/t³`); De Casteljau = the stable eval three.js does NOT use (direct Bernstein for fixed deg 2/3). **Catmull-Rom** = interpolating cubic Hermite, tangent `T_i=(P_{i+1}−P_{i-1})/2` (`(p2−p0)*0.5` in source); **α-parametrisation** `t_{i+1}=[√|Δp|²]^α+t_i` — three.js `pow=α/2` over `distanceToSquared` (centripetal `pow=.25`→α=.5, chordal `.5`→α=1) VERIFIED; **centripetal α=0.5 is the DEFAULT** because Yuksel 2011 PROVES it has no cusps/self-intersections `[CERT-doc]`; `tension` only affects legacy `catmullrom` branch. **NURBS** = knots + Cox-de Boor basis (`NURBSUtils` cites *The NURBS Book* A2.2/A3.1) + **rational weights** (the R): homogeneous accumulate then `divideScalar(w)` → **exact conics/circles** a polynomial curve can't. **Curve API**: `getPoint(t)` (param, non-uniform speed) ≠ `getPointAt(u)` (arc-length frac) — arc length = cumulative chord table (`arcLengthDivisions=200`) + binary-search/lerp in `getUtoTmapping`; `getSpacedPoints` = even spacing. **`computeFrenetFrames` is a rotation-MINIMISING/parallel-transport frame** (NOT true Frenet — avoids inflection flip), propagates N by rotating through inter-tangent angle, distributes residual twist on closed curves (`:421-440`); still jerks on anti-parallel/straight runs → subdivide. **`TubeGeometry`** = circle `P+r(cosN+sinB)` swept via `getPointAt`+frames. **Honest**: NURBS is `examples/jsm` ADDON not core; `NURBSSurface` has NO base class — bare point evaluator, you grid-sample+triangulate yourself. HVAC rule: curved pipes → `CatmullRomCurve3` centripetal + `TubeGeometry`, use `getSpacedPoints` for uniform weld/insulation spacing, NURBS only for exact conics/CAD interop. G64 CLOSED |
| 54 | GPU-budget rendering for the client visor: the levers that cut GPU cost without losing quality | [block54](threejs-block54.md) | **DOCUMENT-mode capture** (§20), "design tools/toolchain" axis, sibling of B52/B53. Scopes GPU-cost levers to the **client-facing browser visor (real GPU)** — our headless QA is SwiftShader (CPU raster, [Block 26]) and is UNAFFECTED. **Top-5 ranked** (savings × applicability ÷ cost): (1) **render-on-demand** — drop `requestAnimationFrame` loop, `controls.addEventListener('change', render)`; manual "render only when something changes … continuous is a waste" `[CERT-web]`; caveat = OrbitControls `enableDamping` needs the damping tail kept rendering (three.js issue **#23090** `[CERT-a]`); R3F `frameloop="demand"`+`invalidate()`. (2) **KTX2/Basis GPU-compressed textures** (`KTX2Loader.detectSupport(renderer)` BEFORE load, `GLTFLoader.setKTX2Loader/setDRACOLoader/setMeshoptDecoder` all confirmed in dev docs `[CERT-web]`) + Draco + meshopt; offline `gltfpack -cc -tc`; stays compressed in VRAM ⇒ ~4-6× less than decoded RGBA `[INFER]`; biggest lever, compounds across repeated units. (3) **instancing** — `InstancedMesh(geom,mat,count)`+`setMatrixAt` (N units→1 draw call), `BatchedMesh` = multi-draw "same material, different geometries" `[CERT-web]`. (4) **shadow control** — `LightShadow.mapSize` (default (512,512)) cap, `autoUpdate=false`+`needsUpdate=true` freezes static-rig shadows (docs verbatim `[CERT-web]`). (5) **DPR cap** `setPixelRatio(min(dpr,2))` + shared Material/Geometry + `mergeGeometries`; frustum culling on by default `[CERT-web]`. **Bigger investment**: baked lightmaps (`MeshStandardMaterial.lightMap` LinearSRGB + `aoMap` red-channel, BOTH need a 2nd UV set `uv1`/`texture.channel=1` `[CERT-web]`) — "free at render time" for STATIC shells only. **Does NOT apply**: `THREE.LOD` (`addLevel(obj,dist,hysteresis)`+`update(camera)` `[CERT-web]`) SKIP for close-inspected single equipment — reconsider only for receding rows/plant floors ([Block 40]); low-repetition instancing; baked lighting on dynamic objects. Gates: `renderer.info.render.calls`<~100, DPR≤2, KTX2 mandatory, shadows frozen, stats-gl frame-time. STAGED (visor shell + asset-pipeline deltas, user promotes). |
| 55 | Turf.js is geospatial, not planar-CAD: the offset/boolean libraries to use for DXF→mesh reconstruction | [block55](threejs-block55.md) | **DOCUMENT-mode capture** (§20), "design tools/toolchain" axis, sibling of the B45/B47/B48 planar-geometry line. Our CAD→3D pipeline (DXF via `ezdxf`→earcut→Three.js) needs planar **offset** (inset/outset walls), **boolean** (holes/courtyards), cleanup, area/centroid — in **planar CAD metres**. **VERDICT: skip Turf.js as a geometry layer; reference it only as a pointer to Cartesian libs.** **The crux**: Turf operates on GeoJSON `[lon,lat]` WGS84 — `@turf/buffer` deps `@turf/jsts`+`@turf/projection`+`d3-geo` (projects→JSTS offset→reprojects, `units` default km) `[CERT-web]`; `@turf/area` = "geodesic area in square meters" (spherical) `[CERT-web]`. Turf issue **#1750**: on planar metres `[0,0]`→`[100,0]` measures **99.88 DEGREES not 100 m** `[CERT-a]`. No planar mode ⇒ **buffer + area are wrong-by-design for CAD**. **Planar-safe in Turf**: `union/difference/intersect` delegate to a Cartesian clipper (`package.json` dep = **`polyclip-ts`**, the maintained TS fork of mfogel `polygon-clipping` — CORRECTION to "delegates to polygon-clipping" `[CERT-web]`); `simplify`/`clean-coords`/`kinks` coord-agnostic; `centroid` = "mean of all vertices" (NOT area-weighted) `[CERT-web]` — usable but you pay the GeoJSON-wrapping tax. **Right tools**: OFFSET = `js-angusj-clipper` (WASM Clipper port, `ClipperOffset`+4 booleans, caveat = integer coords→scale ×1e4 in/out `[CERT-web]`); BOOLEAN = `polygon-clipping` (mfogel, MIT) or `martinez` (README bench `Hole_Hole` **29,530 vs JSTS 2,051 ops/s** `[CERT-web]`); ONE-LIB = `jsts` (`BufferOp`+boolean on floats, slower); area/centroid = hand-rolled shoelace. **License CORRECTION**: `jsts` = **`(EDL-1.0 OR EPL-1.0)` NOT MIT** `[CERT-web]` (`package.json`); js-angusj-clipper MIT (Clipper core Boost), polygon-clipping/martinez MIT. Thin scale-in/out adapter; payoff = avoiding the geodesic distortion. Cross-ref [Block 45]/[Block 47]/[Block 48]. **§7 APPLICABILITY (2026-08-07 scoping)**: the live reconstruction `investigacion/nave-panccadia/tools/build-viewer.py` is **Python + shapely/GEOS** and ALREADY does offset/boolean/holes (`unary_union` `:702`, `.difference` `:706`, `.buffer` `:495`, `Polygon(rings[0],rings[1:])` `:492`) — the §4 JS libs apply ONLY to hypothetical browser-runtime geometry; the current precompute-to-JSON pipeline needs NOTHING from this block (Python peer would be `pyclipper`, not a JS port). |
| 56 | Measured GPU-budget of the nave-Panccadia equipment visor + the damping-safe render-on-demand implementation | [block56](threejs-block56.md) | **DOCUMENT-mode capture** (§20), "design tools/toolchain" axis, sibling of B54. CLOSES the [Block 54] next-step ("measure before applying the GPU playbook; decide from data"). MEASURED the 18 standalone equipment inspectors (`disenos/nave-panccadia/equipos/*/*.html`) via headless `chrome-headless-shell`+`--use-angle=swiftshader` driven over CDP with Node's built-in WebSocket (`sources/probes/B56-visor-perf/measure.mjs`, reproducible), reading `renderer.info` `[CERT]`. **Validity boundary**: draw-call/triangle/geometry/texture/program COUNTS are rasteriser-independent (real `[CERT]`); frame-time is NOT (SwiftShader is CPU-fill-bound → gate (e) UNMEASURED, needs physical GPU + stats-gl) `[INFER]`. **Readout** (`measure-18.csv`): draw calls **15–87 (avg 37.5, worst camara-fermentacion)**, triangles max 6246, textures 2–3 (all procedural `CanvasTexture`, 0 GLB/PNG), `shadowMap.autoUpdate=false` on all 18; `geometries ≈ draw calls` ⇒ no sharing/instancing/merge. **Gates vs B54 §5**: (a) <100 draws → PASS all 18; (b) DPR≤2 → PASS; (c) KTX2 → N/A; (d) shadows frozen PASS but `mapSize 2048`>1024 uniform; (e) real frame-time → UNMEASURED. **VERDICT**: the per-unit visor is already inside the GPU budget — instancing/KTX2/LOD/merge are premature optimisation here (B54 §4 low-repetition caveat fires). **The one data-justified lever = render-on-demand** (B54 §2.1): all 18 run a continuous `requestAnimationFrame` loop over a static (autoRotate-off) scene. Correct DAMPING-SAFE form is the manual's `renderRequested` flag + `requestAnimationFrame` idiom (NOT calling `render` from the `change` event — that re-enters infinitely; open issue **#23090**, proposed `setScene/setRenderer` API never merged) `[CERT-web]`/`[CERT-a]`; the per-equipo wiring a blind port misses = AUTO-ROTATE button, part-visibility toggles, and resize must each call `requestRenderIfNotRequested()`. Trivial cleanup: `mapSize 2048→1024`. Both STAGED shell-template deltas (user promotes); read-only, no prototype edited. Cross-ref [Block 54]/[Block 26]/[Block 40]. |
| 57 | The industrial cold-room door: measured dimensional envelope, hardware vocabulary, and the one number the datasheets refuse to fix | [block57](threejs-block57.md) | **RUN 10, NEW AXIS** — real-world DIMENSIONAL references for the `disenos/catalog/` build (prior runs studied the RENDERER; this studies the SUBJECT). Closes **G67 (doors)**. Sources = DAN-doors manufacturer datasheets, preserved + text-layer extracted (`sources/B57-catalog-dims/dan-doors-MH1001K\|MH1001F\|MH0601K.pdf`). **Certified envelope**: leaf insulation **100 mm PIR**; **max 1400 × 2500 mm** per leaf (2-leaf MH1002K = 2800 × 2500, i.e. 2×1400 — width doubles by adding leaves, height ceiling 2500 never moves); frame **1,5 mm AISI 304**, always insulated (mineral wool); leaf skin **1 mm** AISI 304 / galvanized / white PVC; hinges stainless, height+side adjustable, frictionless bushings (COUNT unstated); sealing TPE-rubber or silicone mounted in the FRAME, tool-free replacement; handles **Fermod 920/921**; kick plates `door width × 300 mm` / `× 800 mm`; window standard `150×300`, `400×600`, `500×600 mm`. **Freeze-vs-chill delta is GEOMETRIC** (MH1001F): heating cables in frame+leaf, and a salt-water-resistant anodised-aluminium **bottom rail cast into the floor** (omittable variant → model as a toggle) — a chill door has no rail. **The honest negative**: every datasheet publishes a MAXIMUM and no nominal size ("solutions exceeding … can be custom made"); a web sweep found no manufacturer-fixed standard. So a spec may claim `confidence: high` only for thickness/gauges/max-envelope/kick-plate/window; the chosen nominal leaf (1.20 × 2.20 m) is `[INFER]`/med by construction. Fermod's latch class is for **overlapping** doors ⇒ leaf laps the frame face, gasket compressed between them (NOT flush-in-reveal). Material rule: AISI 304 skin = bare metal 0.9–1.0, TPE gasket = dielectric 0.0 — the gasket must not inherit the steel material or the seal line vanishes. Cross-ref [Block 29], [Block 22]/HANDBOOK §3.1, [Block 56]. |
| 58 | Selective pallet racking: the rack is SHORTER than the pallet, and a single bay is not a legal rack | [block58](threejs-block58.md) | **RUN 10** sibling of B57. Closes **G68 (storage/racking)**. Sources = SSI SCHÄFER general-catalogue extracts, two DIFFERENT systems kept separate (`ssi-schaefer-pallet-racking-2018.pdf` = **PR 350**, `…-2022.pdf` = **PR 600**) + EUR-pallet refs. **Pallet first** (a rack has no dimensions of its own): UNE-EN 13698-1, **800 × 1,200 × 144 mm**, ≈25 kg, 1.5 t dynamic / 4 t static. **Counter-intuitive core finding**: rack depth **1,050 mm** carries a **1,200 mm** pallet — the pallet OVERHANGS the frame by 75 mm front and back (load rests on two beams, not a shelf); a frame deeper than the pallet reads as shelving, not racking. Transverse storage inverts it (800 mm insertion → 850 mm frame). **Beams**: lengths 1,800/2,200/2,700/2,900/3,300/3,600/3,900 mm; profile number = section height (INP 80/100/120, CE 80/110/140 → beams are 80–140 mm DEEP members); load/pair at 2,700 mm from 1,700 kg (CE 80) to 4,500 kg (INP 120); same INP 100 drops 4,500→2,000 kg from 1,800→3,900 mm. **Uprights** (PR 600) 75/90/100/120 mm wide, frame to 30,000 kg. **Hole pattern 50 : 50 mm** in both systems ⇒ beam levels adjust on a 50 mm grid; bolt-free "welded 5-hook rear lips". **Colour is specified**: beams RAL 5010 gentian blue, uprights galvanized / RAL 7037 dust grey — a uniform grey rack is wrong; and the painted beam is a DIELECTRIC (metalness ≈0) while the galvanized upright is bare metal. **DGUV 108-007 geometry** (`2022 :p.5`): row ≥ **3 bays**, ≥ **2 beam levels**/bay, compartment ≤ **2,500 mm** and levels within **200 mm** of each other, end frame +500 mm, clear passage ≥ 2,000 mm, corner protectors mandatory at free-standing end frames, mesh decks required only when skids run parallel to beams. ⇒ `catalog.yaml:40`'s singular *"bay"* is not a real-floor configuration: model 2–3 bays or label it a section cut. **Derived default** `[INFER]`: 2,700 mm beam = 3 EUR pallets + 4 × 75 mm gaps. **One source REJECTED and recorded**: `dexion-p90-silverline.pdf` has exactly ONE "mm" token in 8 pages — photographic brochure, kept for auditability, cited by no claim. Cross-ref [Block 57], [Block 29], [Block 56]. |
| 59 | Sectional and roll-up industrial doors: the leaf is a chain of standard sections, and the box above the opening is a computed dimension | [block59](threejs-block59.md) | **RUN 10**, third of the dimensional axis (after B57 doors / B58 storage). Closes **G69 for 2 of its 3 subjects**. Sources = Hörmann industrial-sectional 42 mm technical manual + Angel Mir roller-shutter catalogue, preserved + extracted. **Common thread**: unlike B57's rigid hinged leaf, both leaves are NON-RIGID (a panel chain, a coiling curtain), and what a modeller gets wrong is not the leaf but the region ABOVE the opening. **Sectional** (`SPU/APU F42`): panel depth **42 mm** double-skinned PU-foamed hot-galvanized; standard section heights **375 / 500 / 625 / 750 mm** with a **750 (std) or 1500 mm** bottom section ⇒ leaf height is a whole number of standard sections, not a free value; finger-trap protection on every section; glazing = 26 mm S2 double pane; counterbalance = torsion springs + side carrying cables, ≥25,000 cycles (≥50,000 on the heavier tracks); **track application** is the real geometry choice (N/ND/NS/NK/NA/NH/GD/GS · L/LD low-headroom · H/HA/HU high-lift · V/VS/VU/WS/WG vertical · RD/RS/RK), most limited to **RM ≤ 5000 mm**, vertical needs RM ≥ 2200 ⇒ model the ceiling rails + curved transition + spring shaft, or half the object is missing. **Roll-up**: lath **80/100 mm**; guides **G105** (G 55, J 110) and **G135** (G 85, J 135); drum box B **300 / 365-435 / 415-485 / 465-535 mm** with coil R **320-500 mm**; margins C1 50-100, C2 80-200, C3 30-120; max 5,000×5,000 (6,000×6,000 intensive); crank under H 3000, chain at/above. **The equation worth stealing**: `minimum total height = H + B + C1` — a 3,000 mm opening needs 3,435 mm of wall with the small flag. Drum cover is COMPULSORY under H = 2,500 mm. **Security door NOT covered** — no certifiable datasheet found → re-scoped to **G71 blocked-on-thin-source**, with a `tried:` list. **Two more sources REJECTED and recorded** (Hart Doors, Bifold Rolfe: zero "mm" tokens each) ⇒ generalised rule: a brochure is not a dimensional source, the *technical manual* is. Cross-ref [Block 57], [Block 58], [Block 56]. |
| 60 | Access control: the turnstile is fully published, the barrier only half is | [block60](threejs-block60.md) | **RUN 10**, fourth of the dimensional axis. Closes **G72**. **The point of the block is the ASYMMETRY, recorded instead of levelled.** TURNSTILE (Shancharm XC-AT103 datasheet, preserved) is fully specified: housing **450 × 420 × 980 mm**, channel **500-550 mm**, arm **510 mm**, 25 kg, **304 stainless** brushed, DC 24 V solenoid, arm drops on power loss ⇒ it is a WAIST-HEIGHT pedestal (980 mm), and arm ≈ channel width so the three 120° arms sweep the whole gap. BARRIER is only partly published: boom **2.50-8.30 m**, opening **1.5 s (2 m) to 6 s (8 m)**, continuous duty, 24 V/240 W `[CERT-web]`, plus the FAAC manual's **610 mm keep-clear** rule around the swept beam `[CERT-doc]` — but NO reachable vendor source gives a cabinet envelope, so housing height/width/depth stay `[INFER]`/low with a `tried:` list. Refines B59's lesson: a *brochure* is not a datasheet, and neither is a *manual* — an install/programming manual can be primary and still carry no dimension table. Spawns **G73** (sliding gate + personnel airlock, unsourced). Cross-ref [Block 57], [Block 59]. |

### Layer 10 — Core Three.js library: numerical methods & core math (session-A 2026-08-09)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 125 | Numerical methods & core math for r160 (design3d) | [block125](threejs-block125.md) | `Vector3.project` near-plane sign trap (clip-w ≤ 0 flips NDC); robust bbox→NDC with Frustum prefilter; `Box3` containsBox/intersectsBox semantics; edge-manifold valence test (O(F)); signed-volume winding check; adaptive tessellation `round(2πr/L)` vs the common half-circle undercount; `getNormalMatrix` required under non-uniform scale; `Quaternion.setFromUnitVectors` antiparallel guard + slerp short-arc negation; `VectorKeyframeTrack` quaternion shrink → use `QuaternionKeyframeTrack`; Euler gimbal lock threshold `|m13|≥0.9999999`; `getUtoTmapping` binary-search arc-length reparam; Frenet-frame flip at zero curvature → Wang 2008 RMF; **`scene.environmentIntensity` is INERT in r160** (zero hits in build — use `material.envMapIntensity`); IBL + `ColorManagement`/`outputColorSpace` path; `AgXToneMapping` (enum 6) for dark metal; `RectAreaLight` requires `RectAreaLightUniformsLib.init()` or silently skips rendering; physical light units since r155 (`decay:1` → over-bright); superquadrics; centripetal Catmull-Rom for pipes; SDF smooth-min fillets; OBB via PCA for long thin subjects; `geom-verify.mjs` + `adaptive-segments.mjs` + pipe-run wiring shipped |
| 126 | Curved/rounded equipment geometry for r160 (design3d) | [block126](threejs-block126.md) | `LatheGeometry` x>0 constraint + tri formula `2·(N−1)·S` (S=16,N=8→224); `RoundedBoxGeometry(segments=1)` sweet spot (108 tris, user-1→internal-3, default 300 tris); `ExtrudeGeometry` bevelSegments=1 sweet spot; `three-subdivide` Loop subdivision (external MIT, removed r125), `LoopSubdivision.modify(geo,iter,{maxTriangles})`, 4×tris/pass, r160 compat [INFER]; **round-edge normalMap on 12-tri BoxGeometry saves ~89% tris vs RoundedBox(seg=1)**; `material.normalMap + tex.colorSpace=NoColorSpace`; `MeshMatcapMaterial` SwiftShader-safe shortcut; PBR colorSpace table (map=SRGB; normal/rough/metal/ao=NoColorSpace); roughnessMap GREEN channel; metalnessMap BLUE channel; `aoMap` needs `uv1` 2nd UV set; `CanvasTexture` procedural roughness; `CatmullRomCurve3 centripetal` + `TubeGeometry` radialSegments=8 → 90° elbow ~160 tris; `CapsuleGeometry(r,h,capSeg=4,radSeg=8)` r160 core since r139; SphereGeometry correct tri formula `2·W·(H−1)` (default 32×16=960; sweet spot 16×8=224); CylinderGeometry default radialSegments=32 (use 12–16); on-screen-size 4-tier decision rule; adopt shortlist: round-edge normalMap [kit], LatheGeometry [recipe], RoundedBox(seg=1) [recipe], CanvasTexture PBR pack [kit] |

---

## Pending (gap-backlog)

> Readable mirror of `RESEARCH-STATE.md`. Consumed by the loop to pick the next gap.

- [x] G1 — Baseline: how the prototype corpus uses three.js → [Block 1]
- [x] G2 — InstancedMesh & voxel-scale rendering → [Block 2]
- [x] G3 — PBR material system → [Block 3]
- [x] G4 — Lighting & environment (IBL) → [Block 4]
- [x] G5 — Shadows → [Block 5]
- [x] G6 — Color management & tone mapping → [Block 6]
- [x] G7 — Cameras & controls → [Block 7]
- [x] G8 — Geometry toolkit for realistic modeling → [Block 8]
- [x] G9 — Procedural texturing → [Block 9]
- [x] G10 — Migration & versioning → [Block 10]
- [x] G11 — Post-processing upgrade path → [Block 18]
- [x] G12 — Performance → [Block 11]
- [x] G13 — Asset pipeline → [Block 19]
- [x] G20 — Case studies III → [Block 21]
- [x] G22 — PBR value references → [Block 22]
- [x] G23 — Product-lighting design + RectAreaLight → [Block 23]
- [x] G24 — Cheap visual wins (matcap, baked AO, blob shadows) → [Block 24]
- [x] G25 — gltf-transform optimization pipeline → [Block 25]
- [x] G26 — DYNAMIC baseline → [Block 26]
- [x] G27 — Performance budgets per device class → [Block 27]
- [x] G28 — Blender round-trip (medium, run 4) → [Block 28]
- [x] G29 — HVAC equipment visualization domain → [Block 29]
- [x] G30 — Dashboards & telemetry → [Block 30]
- [x] G31 — Terrain/relief → [Block 31]
- [x] G32 — Buildings/BIM → [Block 32]
- [x] G33 — The template as a system (design library architecture) → [Block 33]
- [x] G21 — Forum intelligence → [Block 20]
- [x] G16 — Rendering methods deep-dive → [Block 13]
- [x] G17 — Optimization compendium → [Block 17]
- [x] G18 — External case studies → [Block 14] + [Block 15]
- [x] G19 — MapLibre GL JS interop → [Block 16]
- [x] G15 — BatchedMesh evaluation → closed by remittance, [Block 11] §11.3
- [x] G14 — SYNTHESIS → [Block 12] + WORKFLOW.md
- [x] G40 — LOD applied & measured (run 7, reopen) → [Block 40]
- [ ] G41 — Equipment LOD (dual hi/lo InstancedMesh for chillers/pumps) → **requires-execution (§19)**, not read-only
- [x] G42 — Voxel→realistic parity map + living environment It.1 (relief, animated sea, biomes, sky) → [Block 41] (run 8, build-phase §19)
- [x] G43 — Living environment It.2 (vegetation, paths/boardwalk, beach & pool furniture) → [Block 41 §41.6] (run 8, +12 draws, deployed)
- [x] G44 — Voxel-parity It.4 (scene labels + pipe connectivity fixes [CHW riser to roof AHU, pool loop, condenser 2-pipe] + rooftop AHU->shaft duct) → [Block 41 §41.6] (run 8, deployed). DEFERRED (optional next batch): zone isolation (AISLAR), per-unit detail panel (needs hitboxes on merged geo), pipe flow particles (voxel had them stripped — realistic already at parity).
- [x] G45 — Per-floor average temperature It.3 (simulated °C, HUD panel + 8 floating color-coded labels, 1 Hz) → [Block 41 §41.6] (run 8, +8 draws, deployed)
- [x] G46 — Biome polish + floating-equipment fix + pool machine-room (voxel palette, smooth blends, multi-rect BUILT, pool hut) → [Block 41 §41.7] (run 8, deployed)
- [x] G47 — Room selection + predictive-maintenance panel (88 aligned hitboxes, EdgesGeometry outline by health, temp/setpoint/humidity/amperage + DIAGNOSTICO PREDICTIVO) → [Block 41 §41.7] (run 8, deployed)
- [x] G49 — Sea rework (4 decorrelated directional waves, no global beat) + no-cache header so redeploys are visible → [Block 41 §41.7] (run 8, deployed)
- [x] G48 — Dashboard predictive section (dashboard-energetico-v1.html: 'Mantenimiento predictivo', 7 subsystem health cards, PREDICTIVO ANTES QUE CORRECTIVO banner) → [Block 41 §41.7] (run 8, deployed)
- [x] G50 — Room panel visibility fix: removed #roomPanel from the .clean hide list so it shows on select inside the dashboard iframe (?clean=1) → (run 8, deployed)
- [x] G51 — Selection extended to all 18 equipment units (unified selectUnit, per-type metrics + predictive), panel moved LEFT-center, camera flyTo focus on select + 'VISTA ENFOCADA' badge/vignette with Salir → general → (run 8, deployed)
- [x] G52 — Equipment outlines from real per-type AABB (fix swapped/mis-sized boxes) + persistent warning/alarm highlights (always-on outline, alarm pulses) + badge moved off the views bar → (run 8, deployed)
- [x] G53 — Status bar + warning/danger drill-down ported from Honeywell-MX60 (`ALSER/Proyectos/Pagina/Honeywell-MX60` [CERT], AlarmIndicatorView/HomeMap/StatusResolver): now VERTICAL right-center with per-state click filter (alarm/warning/ok) → popover → row click = selectUnit + focus → (run 8, deployed)
- [x] G54 — Dashboard PDF report: print-to-PDF (@media print, dependency-free) + jsPDF direct download (CDN, offline fallback) of the collected data (health summary, KPIs, predictive subsystems) → (run 8, deployed)
- [x] G55 — UI de-overlap + full responsive pass (distinct panel zones; #temps off left-center; #sys/#ctl/#ctl2 stacked; tablet/phone breakpoints, #roomPanel/#statusPop bottom-sheets <=600, hide secondary <=520) + SEA reverted to STATIC (wave animation looked bad; client asked for static) → (run 8, deployed)

## Non-investigable gaps (without lab / hardware / NDA)

- (none)

## Estimated coverage

40/40 read-only gaps closed across 7 runs — RUN 7 COMPLETE (reopen §8, B40 applied+measured LOD).
1 requires-execution gap queued (G41 equipment LOD, §19 build phase). Terminal: Spanish designer handbook.
- [x] G56 — Per-unit DETAIL VIEW pattern analysis (MX60-web + chihuahua/Niagara): dedicated page = header+KPI cockpit+focused single-unit 3D+Chart.js trends; portable (Niagara only binds the data source) → [Block 42], port plan pending user go-ahead
- [x] G57 — Casino BMS reference (niagara-casino React/Vite) analysis + consolidated hotel detail-OVERLAY build plan (overlay over persistent scene; focused per-unit mini-3D via existing builders; Chart.js trends+comfort bands+ayer baseline; insights engine) → [Block 43]
- [x] G58 — Hotel detail OVERLAY It.A (focused mini-3D via existing builders + KPI cockpit) + cross-iframe chrome-hide + full 3D-UI restyle to B11 Industrial (paper/navy/rust, IBM Plex, flat) + scene labels removed + navy blueprint viewer bg → [Block 43], deployed

### RUN 9 — procedural generation & numerical methods for design tools (new axis, 2026-08-07)

- [x] G59 — Robust polygon offsetting + straight skeleton (wall-union, corner/roof closure) → [Block 45]
- [x] G60 — Robust boolean CSG on 3D solids (three-bvh-csg) → [Block 46]
- [x] G61 — Triangulation: earcut vs Constrained Delaunay for holed polygons (the cap step) → [Block 47]
- [x] G62 — Mesh simplification via Quadric Error Metrics (LOD/decimation) → [Block 49] (premise corrected: SimplifyModifier = Melax then meshopt wrapper; decimate build-time w/ meshoptimizer)
- [x] G63 — Isosurfaces: marching cubes vs dual contouring (three.js MarchingCubes) → [Block 50]
- [x] G64 — Curves & surfaces: Bézier/Catmull-Rom/NURBS (three.js Curve API, NURBS addons) → [Block 51]
- [x] G65 — Numerical robustness of geometric predicates (orientation/incircle, epsilon/snapping) → [Block 48]
- [x] G66 — Procedural placement: poisson-disk/blue-noise + WFC / L-systems → [Block 52] (LAST read-only gap; RUN 9 read-only set EXHAUSTED = STOP)

### RUN 10 — catalog family evidence (robotica worktree, 2026-08-08)

- [x] G74 — ROBOTICS dimensional + joint-hierarchy reference (ABB IRB 6700-235/2.65 envelope & axis
  ranges; UR10e certified DH chain; Robotiq 2F-85 adaptive linkage; MiR250 AMR envelope; ABB IRB 660
  4-axis palletizer + parallelogram constraint; ISO 13857 fence heights + SICK C4000 light-curtain
  proportions; the six-rule joint-hierarchy contract for three.js) → [Block 62]
