<!-- review-status: pending -->
# nave-3sistemas — P8 retrospective (2026-08-15)

**Scope:** the lighting-camera pass, driven from the accepted blockout through a blind-gate cycle
(attempts 1-5) to a unanimous 2-judge panel PASS (0.79). Also covers the offline retrofit + dev-UI
that opened the session.

**Honesty note:** this retro is written INLINE by the orchestrator, not delegated to a fresh-context
agent (SELF-IMPROVEMENT §Run-end step 0 prefers delegation for non-quick runs). Every claim below is
pinned to an on-disk artifact (a review JSON, a commit, a score in progress.yaml), so it is evidence,
not self-rationalization — but a fresh reviewer verifying against those artifacts is still welcome.

## What happened (evidence-pinned)
- Offline retrofit + dev-UI landed and were render-verified (commits cc39bf7).
- DC1/DC2/DC3 (camera/light) corrections landed; blind gate attempt 1 CONFIRMED them but FAILed 0.70
  on crude blockout geometry (`lighting-camera-attempt1.review.json`).
- User chose to detail the geometry. Attempt 2 took enclosure/skid/bay to PASS but luminaire-grid
  REGRESSED to 0.55 (`...attempt` history in `progress.yaml`).
- Attempt 3 (finned UFO highbays) → PASS 0.79 single judge.
- DC6-DC8 polish triggered a confirmation re-gate (attempt 4) that FAILed ahu-and-duct-run 0.71: the
  round HVAC trunk was indistinguishable from the round compressed-air pipe (DC5).
- DC5 fix (rectangular trunk, commit 191674b) → attempt 5 UNANIMOUS 2-judge panel PASS 0.79
  (`lighting-camera-panel.review.json`).

## Proposed kit deltas (propose-only — human applies)

| # | change | target file/§ | evidence | type | priority |
|---|---|---|---|---|---|
| 1 | After ANY geometry/material change, re-gate the WHOLE evidence set, never just the changed feature — a fresh judge re-scores untouched features too, and a marginal PASS can flip. | GATES.md §Gate steps (re-capture scope) | luminaire-grid PASS 0.76 (att1) → FAIL 0.55 (att2) with the luminaire geometry UNCHANGED between them | clarification | medium |
| 2 | Escalate the FINAL verdict to a 2-judge panel when any feature has BOUNCED across attempts (not only after N same-feature failures). Single-judge variance at the margin is real. | GATES.md §Blind-review protocol (panel trigger) | luminaire-grid swung 0.76↔0.55; ahu-and-duct-run 0.76↔0.71 across single judges; the panel gave a stable unanimous 0.79 | addition | medium |
| 3 | Differentiate co-located services by CROSS-SECTION/shape, not material alone — two round cylinders read as one system even with different materials + markers. | TRACK-THREEJS §Generic defaults or a new §Legibility note | ahu-and-duct-run 0.71 FAIL (round trunk vs round air pipe) → PASS after the trunk became rectangular (191674b) | addition | low |

## Already covered by the kit (dedupe)
- Framing-to-NDC, occlusion coverage, native-crop-cuts-both-ways, dark-metal-lighting: all present in
  `LEARNINGS.md` §Active and the geom-verify harness. This run re-exercised them; nothing new there.

## Honesty clause
The run surfaced three genuinely new deltas above and a pile of already-covered ones. It did NOT invent
a defect to look productive — the three are each pinned to a score swing or a commit.
