passes:
  blockout:
    status: failed(3)
    attempts: 3
    score: 0.78
    action: refine-code
    screenshot: runs/blockout-attempt3.png
    review: runs/blockout-attempt3.judge-B.json
    panel:
      judge_a_presence: {verdict: PASS, global: 0.81, skid: 0.82}
      judge_b_legibility: {verdict: FAIL, global: 0.78, skid: 0.71}
      judge_c_promise: {verdict: FAIL, global: 0.75, skid: 0.73}
      majority: FAIL
    cost:
      writer_min: 96
      capture_min: 34
      review_min: 31
      writer_tokens: 550000
    note: >-
      failed(3) — the kit's stop point. Attempt 3 was judged by a THREE-JUDGE PANEL with different
      lenses, escalated per GATES.md after the same critical feature failed twice.

      Four of five critical features PASS on all three judges. bay-structure went 0.77 -> 0.70 ->
      0.82/0.83/0.87 once the trusses were split out of the roof-deck layer, which was the real
      code defect the first two attempts could not see.

      The single blocking feature is compressor-skid-identity, and the panel is UNANIMOUS about the
      cause even though it split on the verdict: the geometry is present and correct. Judge A proved
      it with five native-resolution crops (fan disc with raised hub ring, receiver with domed top,
      dryer, header pipe) and still filed the defect that matters most: "presence is not in doubt,
      but the feature requires cropping to read, which undermines the gate capture's
      self-sufficiency."

      So all three failures across three attempts were EVIDENCE failures, each a different layer of
      the same problem: attempt 1 the packages were plain boxes; attempt 2 they were detailed but
      shot from too far for a downscaled render to carry; attempt 3 they were framed close but lit
      too dark, and the reframe pushed the receiver and dryer out of shot.

      Corrections PREPARED but NOT yet spent as an attempt (a run never authorises its own fourth
      attempt or its own lineage reset — GATES.md):
        1. compressor-skid preset re-placed on the key-light side at (19.5, 5.5, 2.5) with fov 36,
           framing all four skid elements instead of only the two cabinets.
        2. A scoped inspection fill declared on that preset alone, so the house rig every other shot
           is judged under stays untouched.
        3. A new `luminaire-plan` preset down the short axis, so 18 fixtures can be COUNTED rather
           than inferred from regularity — two judges could only enumerate 15-17.
      None of the three touches the model.

      USER DECISION 2026-08-13: skip ahead to the dashboard. The blockout is accepted as a
      sufficient base — its massing, proportions and part decomposition are gate-evidenced, and the
      one open feature is a presentation defect with its fix already staged, not missing geometry.
      The 3D gets its polish in the materials and lighting-camera passes, where the compressor-skid
      lighting correction lands naturally. Recorded here so a later reader does not mistake an
      accepted deferral for an unnoticed failure. The three prepared corrections stay in
      DEFERRED-CORRECTIONS.md and are owned by the lighting-camera pass.
