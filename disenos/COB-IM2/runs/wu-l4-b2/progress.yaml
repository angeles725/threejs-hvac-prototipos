passes:
  caps:
    status: passed
    attempts: 1
    score: 0.83
    action: continue
    screenshot: runs/wu-l4-b2/caps-attempt1.png
    review: runs/wu-l4-b2/caps-attempt1.review.json
    note: >-
      full-3d stencil section caps + loft caps, group.renderOrder=0 from the start.
      Gated: capdebug=1 magenta exactly at cuts (positive) + capdebug=1 no-clip = no magenta
      (negative control) + ?clip=3.90 normal shows solid cap faces. @3D added ?clip flag
      (was missing, blocking URL-reproducible cuts). Passed first attempt (B1 pattern reused).
