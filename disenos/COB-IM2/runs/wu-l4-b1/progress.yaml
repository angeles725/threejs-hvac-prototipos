passes:
  caps:
    status: passed
    attempts: 2
    score: 0.82
    action: continue
    screenshot: runs/wu-l4-b1/caps-attempt2.png
    review: runs/wu-l4-b1/caps-attempt2.review.json
    note: >-
      Stencil section caps over the bay-clip cross-sections + 124 loft end-caps.
      attempt1 no-op (render-order inversion: renderOrder on a Group becomes groupOrder,
      sorted before renderOrder, so caps drew before the count). Fixed group.renderOrder=0.
      Verified by capdebug=1 magenta-at-cuts + before/after cut-region diff 0->169738 px.
