# SURFACE Reset 1 — lineage boundary

**Asset:** shell-circulation-facade  
**Pass:** SURFACE  
**Active lineage:** 1, attempt 1  
**Reset:** surface-reset-1

## Why a new lineage exists

SURFACE lineage 0 exhausted its three allowed correction attempts at global scores `0.68`, `0.65`, and `0.79`. Attempt 3 cleared four of five critical features, but `canonical-network-endpoints` remained below threshold at `0.77 / 0.80`. Continuing as attempt 4 would violate the bounded correction contract, so the exhausted lineage was archived intact under `history/surface-lineage-0/` and this reset began from a fresh attempt namespace.

The archive contains the original PNGs, sidecars, probes, mechanical evidence, review results, reports, logs, score history, rationale, and `INVENTORY.sha256`. No lineage-0 artifact remains in the active `runs/` namespace.

## Reset scope

The reset deliberately preserves the accepted atlas, shell, device geometry scale, materials, network endpoints, and canonical topology. It changes only:

- the ambiguous multi-arm direction clusters, replaced by one terminal-adjacent triangular arrowhead per route;
- complete-network evidence label visibility, scale, and render priority so TC300, UC100, UG67, bus, and external IDs outrank competing zone labels;
- the corridor evidence camera, lowered and centered so room numbers, exits, posters, and the carpet spine remain readable without roof-member obstruction.

SURFACE stays locked pending a fresh blind review. No score or pass verdict is asserted by the implementation thread.

