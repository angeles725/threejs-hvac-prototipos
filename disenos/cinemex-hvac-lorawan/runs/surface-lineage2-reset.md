# SURFACE Reset 2 — lineage boundary

**Asset:** shell-circulation-facade  
**Pass:** SURFACE  
**Active lineage:** 2, attempt 1  
**Reset:** surface-reset-2

## Why a new lineage exists

Surface Reset 1 exhausted its three bounded attempts at global scores `0.79`, `0.75`, and `0.78`. The final blind review again failed only the critical `canonical-network-endpoints` feature (`0.77 / 0.80`): dedicated RS-485 evidence was readable, but the saved complete-network and UG67 compositions did not show the entire TC300 → RS-485 → UC100 → LoRaWAN → UG67 → Ethernet/Internet → Niagara chain simultaneously and without clutter.

The exhausted lineage is preserved under `history/surface-lineage-1/`. Its complete pre-cleanup evidence inventory includes the superseded PNGs and console sidecars; its retained archive inventory contains every JSON, report, manifest, probe, test, syntax and mechanical artifact. This reset is not attempt 4 and does not inherit a score or verdict.

## Reset scope

The reset preserves the accepted building, device placement, routes, topology, materials, architecture presets and device scale. It adds one derived evidence-only `architecture_system_diagram_board`:

- a single 2048 × 840 sRGB canvas texture on a framed 34 × 14 m 3D panel;
- four explicit TC300/RS-485/UC100 bus rows;
- four separated dashed LoRaWAN lanes entering distinct UG67 ports;
- one solid Ethernet/Internet/Niagara/client chain;
- a cyan leader from the real UG67 to the diagram socket, proving that the panel references rather than duplicates the physical device;
- camera-specific evidence visibility that suppresses dense physical routes only in the network overview and restores them in dedicated physical-network views.

SURFACE remains locked while implementation and mechanical evidence are being produced. A fresh independent blind review is required before any pass verdict.
